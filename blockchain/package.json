{
  "name": "finite-earth-blockchain",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:megaeth": "hardhat run scripts/deploy.ts --network megaeth",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
