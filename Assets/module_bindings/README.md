This folder is reserved for SpacetimeDB Unity bindings.

Generate with the SpacetimeDB Unity workflow (example):
`spacetime generate --lang csharp --out-dir Assets/module_bindings`

Bindings are intentionally not checked in here.
